# Implementation Plan

## UI

- Constrain the Scheduled settings status panel, empty state, and job list to a narrower max width.
- Replace the multi-row run history block with a single latest-run timestamp row.

## Copy

- Update `settings.cronJobs` and `settings-scheduled-tasks` i18n labels in all locales.

## Validation

- Run format, i18n, lint, typecheck, and targeted renderer/client tests.

